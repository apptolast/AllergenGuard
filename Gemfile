source "https://rubygems.org"

# Pinned to >= 2.237.0: fastlane 2.236.0 breaks upload_to_testflight/altool
# ("The file couldn't be opened because it isn't in the correct format. (259)")
# — the altool -f/-assetFile flag regression and the base64 .p8 decode bug, both fixed in 2.237.0.
gem "fastlane", ">= 2.237.0"
